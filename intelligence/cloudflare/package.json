{
  "name": "personal-intelligence-cloudflare",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "db:migrate:local": "wrangler d1 migrations apply personal-intelligence --local",
    "db:migrate:remote": "wrangler d1 migrations apply personal-intelligence --remote"
  }
}
