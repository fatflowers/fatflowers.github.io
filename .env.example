# Personal intelligence Worker API. Copy to .env locally; never commit real values.
INTELLIGENCE_API_URL=https://personal-intelligence-api.sunliumin.workers.dev
INTELLIGENCE_API_TOKEN=replace-with-worker-secret

# Optional path overrides (defaults work from the repository root).
# INTELLIGENCE_CATALOG_PATH=intelligence/config/catalog.yaml
# INTELLIGENCE_CATALOG_SCHEMA_PATH=intelligence/schemas/catalog.schema.json
